# 安全声明 / Security Policy

## 支持范围

当前维护版本为 `0.2.0`。安全修复优先针对当前版本和 GitHub Release 中明确标注的版本。

## 漏洞报告

请不要在公开 Issue 中发布可复现的漏洞细节、配对码、本机控制凭据、完整连接 URL、二维码截图、局域网地址、个人文件名或包含个人路径的日志。

优先使用 GitHub 仓库的私密漏洞报告功能。报告时请提供：

- 受影响版本、操作系统和 Python 版本；
- 最小复现步骤以及预期、实际行为；
- 删除敏感内容后的必要日志或截图；
- 影响范围、利用条件和可能的修复方向。

提交前请再次确认材料中不含密码、Token、Cookie、私人文件、个人目录、完整配对 URL 或其他身份信息。报告内容会按最小知情原则处理。

## 运行安全要求

- WindBridge 只面向可信局域网。不要在公共 Wi-Fi、访客网络中启用，不要通过路由器端口映射、反向代理或防火墙规则将服务暴露到互联网。
- 当前移动端页面通过局域网 HTTP 提供，不具备 TLS 传输加密。传输敏感文件前应先自行加密，并通过其他渠道传递密码。
- 二维码和访问地址包含当前配对码。不要截图公开分享；使用结束后可点击“更换配对码”，使旧链接立即失效。
- UDP 发现只广播设备名、局域网地址和端口，不包含配对码或本机控制凭据。
- `%LOCALAPPDATA%\WindBridge\settings.json` 包含本机内部控制凭据，应视为敏感配置。不得上传、粘贴到 Issue，或随调试压缩包公开分享。
- 从浏览器上传到电脑的文件属于不可信输入。应用会清理路径字符并避免覆盖同名文件，但不会查杀恶意内容；打开前应执行安全扫描。
- 资源管理器“发送到 WindBridge”入口只应通过应用设置页安装。若移动或删除主程序，应从设置页移除旧入口并重新安装。
- Windows 防火墙首次提示时仅允许专用网络。不要为了连接方便关闭系统防火墙。
- 不要把 `dist/` 中自行修改的二进制文件冒充官方发布包。正式发布时应提供 SHA-256 摘要。

## 安全设计说明

- 文件和文本直接在设备之间传输，不使用第三方云端中转。
- 应用没有内置遥测、广告 SDK 或远程统计服务。
- 浏览器接口要求随机配对码；更换配对码后旧链接立即失效。
- 资源管理器文件转交接口仅接受来自 `127.0.0.1` 或 `::1` 的请求，并验证独立的随机本机控制凭据。
- 上传文件使用安全文件名并写入用户配置的接收目录；同名文件生成新名称，不覆盖既有文件。
- 单次上传请求限制为 2 GB，避免无边界请求占用磁盘与内存。
- 文件共享列表、文本桥内容和活动记录仅驻留在进程内存中，退出后不会持久化。

## 已知边界

配对码属于局域网访问控制，不等同于端到端加密、用户身份认证或抵御已控制本机的攻击者。可信网络之外的远程访问应通过用户自行管理的加密 VPN 完成，而不是把 WindBridge 端口直接暴露到公网。

---

The maintained version is `0.2.0`. Report security issues privately and remove pairing tokens, full URLs, private filenames, local paths, IP addresses, and other sensitive data. WindBridge is intended only for trusted local networks, has no telemetry or cloud relay, and must not be exposed directly to the public internet.
